

spring:
  application:
    name: passport-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
      profile: dev
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8762/eureka

server:
  port: 0