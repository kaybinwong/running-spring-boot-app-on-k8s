spring:
  application:
    name: gateway-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
      label: master
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8762/eureka/

server:
  port: 0