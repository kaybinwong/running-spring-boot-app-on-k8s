spring:
  application:
    name: config-service

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: D:/OpenSource/running-spring-boot-app-on-k8s/mircoservices/config-repo
#        git:
#          search-paths: mircoservices/config-repo
#          username:
#          password:
#          uri: https://github.com/kaybinwong/running-spring-boot-app-on-k8s

server:
  port: 8088